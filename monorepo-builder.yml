parameters:
    # for "merge" command
    data_to_append:
        require-dev:
            phpunit/phpunit: '^7.3'
            symplify/monorepo-builder: '^4.7'

    # for "split" command
    directories_to_repositories:
        packages/first-package: 'git@github.com:your-monorepo/first-package.git'
        packages/second-package: 'git@github.com:your-monorepo/second-package.git'
    package_directories:
    - 'php-packages'
    - 'wp-content/mu-plugins/plugins'
    parameters:
        merge_sections:
        # default values
        - 'require'
        - 'require-dev'
        - 'autoload'
        - 'autoload-dev'
        - 'repositories'
        # Josh added values
        #  - 'extra'
