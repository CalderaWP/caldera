import React from 'react';
import { HorizontalForm } from './HorizontalForm';
export const Processor = props => <HorizontalForm {...props} />;
Processor.propTypes = HorizontalForm.propTypes;
Processor.defaultProps = HorizontalForm.defaultProps;
